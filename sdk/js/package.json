{
  "private": true,
  "name": "actra-monorepo",
  "packageManager": "pnpm",
  "devDependencies": {
    "tsup": "^8.5.1",
    "typescript": "^5.9.3"
  },
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r exec rm -rf dist"
  }
}