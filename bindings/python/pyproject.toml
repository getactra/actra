[build-system]
requires = ["maturin>=1.4"]
build-backend = "maturin"

[project]
name = "actra"
description = "Deterministic admission control engine"
requires-python = ">=3.9"
license = { text = "Apache-2.0" }
dynamic = ["version"]

[lib]
name = "actra"